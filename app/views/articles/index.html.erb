<h1>All articles</h1>
<%= link_to 'Sort by Tags', tags_path %>
<ul id="articles"></ul>
  <% @articles.each do |article| %>
    <li>
      <%= link_to article.title, article_path(article), class: 'article_title' %>
    </li>
    <%end%>
    <%= link_to 'Create a New Article', new_article_path, class: 'new_article' %>
  </ul>
